package localblockdevicelib

import (
	"nixomose/nixomosegotools/tools"
)

type localblockdevicelib struct {
}

func New_localblockdevicelib(config_file, device_name, size, storage_device_url, communication_protocol) (tools.Ret, *locallocalblockdevicelib) {

	// read config
	// make logger

	var log tools.Nixomosetools_logger

}
